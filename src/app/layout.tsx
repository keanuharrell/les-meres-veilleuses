import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Navigation } from "@/components/Navigation";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Les Mères Veilleuses - Association sportive et solidaire",
  description: "Une association née du cœur pour sensibiliser à la lutte contre les cancers du sang à travers le sport et la solidarité. Suivez notre aventure au Raid Amazones 2025.",
  keywords: "association, sport féminin, cancer du sang, lymphome, solidarité, raid amazones, don, mères veilleuses",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="fr">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        <Navigation />
        {children}
      </body>
    </html>
  );
}
